<Window x:Class="WingetUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WingetUpdater"
        mc:Ignorable="d"
        Title="Winget Updater - Actualizador de Software" 
        Height="750" 
        Width="1200"
        MinHeight="600"
        MinWidth="900"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Winget Updater" 
                          FontSize="32" 
                          FontWeight="SemiBold" 
                          Foreground="{StaticResource TextBrush}"
                          FontFamily="Segoe UI Variable, Segoe UI"/>
                <TextBlock Text="Mantén tu software actualizado de forma sencilla" 
                          FontSize="14" 
                          Foreground="{StaticResource TextSecondaryBrush}"
                          Margin="0,6,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    Content="🔄 Refrescar" 
                    Style="{StaticResource ModernButton}"
                    Command="{Binding RefreshCommand}"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Action Buttons -->
        <Border Grid.Row="1" 
                Style="{StaticResource Card}"
                Padding="20"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        Content="Seleccionar Todo" 
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding SelectAllCommand}"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="1" 
                        Content="Deseleccionar Todo" 
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding DeselectAllCommand}"
                        Margin="0,0,12,0"/>

                <Button Grid.Column="3" 
                        Content="🗑️ Desinstalar Seleccionados" 
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding UninstallSelectedCommand}"
                        Margin="0,0,8,0"
                        Foreground="#D13438"/>

                <Button Grid.Column="4" 
                        Content="⚡ Actualizar Seleccionados" 
                        Style="{StaticResource ModernButton}"
                        Command="{Binding UpdateSelectedCommand}"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="5" 
                        Content="Actualizar Todo" 
                        Style="{StaticResource ModernButton}"
                        Command="{Binding UpdateAllCommand}"/>
            </Grid>
        </Border>

        <!-- Package List -->
        <Border Grid.Row="2" 
                Style="{StaticResource Card}"
                Margin="0,0,0,12">
            <Grid>
                <DataGrid ItemsSource="{Binding Packages}"
                         Style="{StaticResource ModernDataGrid}"
                         ColumnHeaderStyle="{StaticResource ModernColumnHeader}"
                         CellStyle="{StaticResource ModernCell}">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="✓" 
                                              Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                              Width="50"/>
                        <DataGridTextColumn Header="Nombre" 
                                          Binding="{Binding Name}" 
                                          Width="2*"/>
                        <DataGridTextColumn Header="ID" 
                                          Binding="{Binding Id}" 
                                          Width="2*"/>
                        <DataGridTextColumn Header="Versión Actual" 
                                          Binding="{Binding CurrentVersion}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="Nueva Versión" 
                                          Binding="{Binding AvailableVersion}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="Estado" 
                                          Binding="{Binding StatusText}" 
                                          Width="*"/>
                        
                        <!-- Actions Column -->
                        <DataGridTemplateColumn Header="Acciones" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="⚡" 
                                                ToolTip="Actualizar"
                                                Style="{StaticResource SecondaryButton}"
                                                Command="{Binding DataContext.UpdatePackageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,4,0"
                                                Padding="8,4"
                                                FontSize="16"
                                                Foreground="#0078D4"/>
                                        <Button Content="🗑️" 
                                                ToolTip="Desinstalar"
                                                Style="{StaticResource SecondaryButton}"
                                                Command="{Binding DataContext.UninstallPackageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Padding="8,4"
                                                FontSize="16"
                                                Foreground="#D13438"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center" 
                           HorizontalAlignment="Center"
                           Visibility="{Binding Packages.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock Text="📦" 
                              FontSize="48" 
                              HorizontalAlignment="Center"
                              Margin="0,0,0,16"/>
                    <TextBlock Text="No hay actualizaciones disponibles" 
                              FontSize="16" 
                              Foreground="{StaticResource TextSecondaryBrush}"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Haz clic en 'Refrescar' para buscar actualizaciones" 
                              FontSize="13" 
                              Foreground="{StaticResource TextSecondaryBrush}"
                              HorizontalAlignment="Center"
                              Margin="0,8,0,0"/>
                </StackPanel>

                <!-- Loading Overlay -->
                <Border Background="#F0FFFFFF" 
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" 
                               HorizontalAlignment="Center">
                        <TextBlock Text="⏳" 
                                  FontSize="48" 
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="Buscando actualizaciones..." 
                                  FontSize="16" 
                                  Foreground="{StaticResource TextBrush}"
                                  HorizontalAlignment="Center"
                                  Margin="0,16,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Progress Bar and Status -->
        <Border Grid.Row="3" 
                Style="{StaticResource Card}"
                Padding="20"
                Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                          Text="{Binding StatusMessage}" 
                          FontSize="13" 
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextBrush}"
                          Margin="0,0,0,10"/>

                <ProgressBar Grid.Row="1" 
                            Value="{Binding Progress}" 
                            Maximum="100"
                            Style="{StaticResource ModernProgressBar}"
                            IsIndeterminate="{Binding IsUpdating}"/>
            </Grid>
        </Border>

        <!-- Logs Panel (Collapsible) -->
        <Border Grid.Row="4" 
                Style="{StaticResource Card}"
                Visibility="{Binding ShowLogs, Converter={StaticResource BoolToVisibilityConverter}}"
                MaxHeight="250">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                       Background="{StaticResource BackgroundBrush}" 
                       Padding="16,12"
                       CornerRadius="8,8,0,0">
                    <Grid>
                        <TextBlock Text="📋 Detalles Técnicos" 
                                  FontSize="13" 
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource TextBrush}"/>
                        <Button Content="✕ Ocultar" 
                               HorizontalAlignment="Right"
                               Style="{StaticResource SecondaryButton}"
                               Command="{Binding ToggleLogsCommand}"
                               Padding="12,6"
                               FontSize="12"/>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" 
                             VerticalScrollBarVisibility="Auto"
                             Padding="0">
                    <TextBox Text="{Binding Logs, Mode=OneWay}" 
                            Style="{StaticResource LogTextBox}"
                            BorderThickness="0"
                            VerticalAlignment="Stretch"
                            MinHeight="150"/>
                </ScrollViewer>
            </Grid>
        </Border>
        
        <!-- Toggle Logs Button (when hidden) -->
        <Border Grid.Row="4"
                Style="{StaticResource Card}"
                Padding="20"
                Visibility="{Binding ShowLogs, Converter={StaticResource BoolToVisibilityConverter}}"
                Cursor="Hand"
                Background="{StaticResource SurfaceHoverBrush}">
            <Border.InputBindings>
                <MouseBinding Command="{Binding ToggleLogsCommand}" MouseAction="LeftClick"/>
            </Border.InputBindings>
            <Grid>
                <TextBlock Text="📋 Mostrar Detalles Técnicos" 
                          FontSize="13"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
